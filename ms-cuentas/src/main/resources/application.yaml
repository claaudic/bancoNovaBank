spring:
  application:
    name: ms-cuentas

  datasource:
    url: jdbc:postgresql://34.16.103.65:5432/postgres
    username: panel-ciudadano-dev
    password: F^TzfiihrKz^QHy`

  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        default_schema: novabank-cuentas

  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true
    default-schema: novabank-cuentas

server:
  port: 8081

ms-clientes:
  url: http://localhost:8080
