# Diagrama Entidad-Relación

Modelo relacional de Banco NovaBank, separado por microservicio. Cada microservicio mantiene su propia base de datos con schema independiente.

---

## ms-clientes (schema: novabank-clientes)

```mermaid
erDiagram
    CLIENTE ||--o{ DIRECCION_CLIENTE : tiene
    CLIENTE }o--|| PROFESION : pertenece

    CLIENTE {
        string rut_cliente PK
        string numero_serie UK
        string nombre_cliente
        string apellido_cliente
        string telefono_cliente
        string email_cliente UK
        timestamp fecha_creacion
        string estado
        bigint id_profesion FK
    }

    PROFESION {
        bigint id_profesion PK
        string nombre_profesion UK
    }

    DIRECCION_CLIENTE {
        bigint id PK
        string calle
        string numero
        string depta
        string tipo_direccion
        string ciudad
        string rut_cliente FK
    }

    USUARIO {
        bigint id_usuario PK
        string username UK
        string password
        string rol
        timestamp fecha_creacion
    }
```

**Relaciones:**
- Un cliente pertenece a una profesión (`@ManyToOne`).
- Un cliente puede tener múltiples direcciones (`@OneToMany`, cascade).
- Usuario es autónomo (autenticación con BCrypt).

---

## ms-cuentas (schema: novabank-cuentas)

```mermaid
erDiagram
    TIPO_CUENTA ||--o{ CUENTA : clasifica
    CUENTA ||--o{ TARJETA : tiene

    TIPO_CUENTA {
        bigint id_tipo_cuenta PK
        string nombre_tipo_cuenta UK
    }

    CUENTA {
        bigint id_cuenta PK
        string numero_cuenta UK
        date fecha_creacion
        numeric saldo
        string rut_cliente
        string estado
        bigint id_tipo_cuenta FK
    }

    TARJETA {
        bigint id_tarjeta PK
        string numero_tarjeta UK
        date fecha_vencimiento
        string cvv
        string estado
        bigint id_cuenta FK
    }
```

**Relaciones:**
- Una cuenta pertenece a un tipo de cuenta (`@ManyToOne`).
- Una cuenta puede tener múltiples tarjetas (`@OneToMany`).
- `rut_cliente` es un **identificador externo** (no FK JPA) — la validación de existencia se hace vía Feign a ms-clientes.

---

## ms-transacciones (schema: novabank-transacciones)

```mermaid
erDiagram
    TRANSACCION {
        bigint id_transaccion PK
        bigint id_cuenta_origen
        bigint id_cuenta_destino
        string tipo_transaccion
        numeric monto_transaccion
        timestamp fecha_transaccion
        string descripcion
        string estado
    }
```

**Relaciones:**
- `id_cuenta_origen` e `id_cuenta_destino` son **identificadores externos** — validación de cuentas vía Feign a ms-cuentas.
- No hay foreign keys JPA cross-microservicio (respeta el principio Database per Service).

---

## ms-organizacion (schema: novabank-organizacion)

```mermaid
erDiagram
    SUCURSAL ||--o{ EJECUTIVO : asigna
    SUCURSAL ||--o{ DIRECCION_SUCURSAL : ubica_en

    SUCURSAL {
        bigint id_sucursal PK
        string nombre UK
        string email UK
        string telefono
        string estado
        timestamp fecha_creacion
    }

    EJECUTIVO {
        bigint id_ejecutivo PK
        string nombre
        string apellido
        string email UK
        string telefono
        string cargo
        string estado
        date fecha_ingreso
        bigint id_sucursal FK
    }

    DIRECCION_SUCURSAL {
        bigint id_direccion PK
        string tipo_direccion
        string calle
        string numero
        string depto
        string ciudad
        string region
        string referencia
        bigint id_sucursal FK
    }
```

**Relaciones:**
- Una sucursal puede tener múltiples ejecutivos (`@OneToMany`).
- Una sucursal puede tener múltiples direcciones físicas (`@OneToMany`).

---

## Convenciones de la base de datos

| Concepto | Implementación |
|---|---|
| Identificadores numéricos | `BIGINT GENERATED BY DEFAULT AS IDENTITY` (PostgreSQL) |
| Identificadores naturales | `VARCHAR` (ej: `rut_cliente`) |
| Decimales monetarios | `NUMERIC(19, 2)` |
| Enumeraciones | `VARCHAR(15)` con `CHECK` constraint |
| Fechas con hora | `TIMESTAMP WITHOUT TIME ZONE` |
| Fechas | `DATE` |
| Validación de unicidad | `UNIQUE` constraints (`UK`) |
| Integridad referencial | `FOREIGN KEY` interna por microservicio |

---

## Comunicación entre microservicios

```mermaid
flowchart LR
    A[ms-transacciones<br/>:8082] -->|Feign| B[ms-cuentas<br/>:8081]
    B -->|Feign| C[ms-clientes<br/>:8080]
    D[ms-organizacion<br/>:8083]

    A -.- DB1[(novabank-<br/>transacciones)]
    B -.- DB2[(novabank-<br/>cuentas)]
    C -.- DB3[(novabank-<br/>clientes)]
    D -.- DB4[(novabank-<br/>organizacion)]
```

**Flujos:**

- **Crear cuenta:** `ms-cuentas` valida vía Feign que el `rutCliente` exista en `ms-clientes` antes de persistir.
- **Crear transacción:** `ms-transacciones` consulta cuentas en `ms-cuentas`, valida estado ACTIVA, aplica `retirar` y `depositar` vía Feign.
- `ms-organizacion` es autónomo: gestiona la estructura interna del banco sin depender de los demás.
